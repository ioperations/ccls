name: "pre-release"

on:
  push:
    branches:
      - master

jobs:
  pre-release:
    name: "Pre Release"
    runs-on: "ubuntu-latest"

    steps:
      - uses: actions/checkout@v2
      - name: "Build & test"
        run: |
          sudo apt update 
          DEBIAN_FRONTEND="noninteractive" sudo apt install -y lsb-release software-properties-common wget
          wget - https://apt.llvm.org/llvm.sh -o llvm.sh
          DEBIAN_FRONTEND="noninteractive" sudo bash llvm.sh 12
          echo "deb http://apt.llvm.org/focal/ llvm-toolchain-focal main\ndeb http://apt.llvm.org/focal/ llvm-toolchain-focal-11 main\ndeb http://apt.llvm.org/focal/ llvm-toolchain-focal-12 main" | sudo tee -a /etc/apt/sources.list.d/llvm.list       

          sudo apt update 
          sudo apt install rapidjson-dev clang-12 build-essential -y libclang-12-dev
          mkdir build
          cd build
          cmake -DCMAKE_CXX_COMPILER=clang++ ..
          cpack

      - uses: "marvinpinto/action-automatic-releases@latest"
        with: 
          repo_token: "${{ secrets.GITHUB_TOKEN }}"
          automatic_release_tag: "latest"
          prerelease: true
          title: "Development Build"
          files: |
            LICENSE
            build/*.deb
